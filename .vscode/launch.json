{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Executar principal",
            "type": "debugpy",
            "request": "launch",
            "module": "dominio.principal",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
